/*
A pointer is a variable that holds the memory location of another variable.
*/

// memory size occupied by pointers. (for me it's 4(idk why...lets use gpt)).

// #include<stdio.h>
// int main()
// {
//     char *pnum;
//     char *pch;
//     float *pfnum;
//     printf("The space occupied by pnum,pch,pfnum is %d %d %d",sizeof(pnum),sizeof(pch),sizeof(pfnum));
//     return 0;
// }

// #include<stdio.h>
// int main()
// {
//     int x=10;
//     int *ptr;
//     ptr=&x; //ptr is now holding the address of variable x as its value and itself has an address that is associated with itself.
//     printf("The adresses of x and ptr are %d %d",&x,&ptr);
//     printf("The value stored at the address %d holded by ptr is %d",ptr,*ptr);
//     return 0;

// }